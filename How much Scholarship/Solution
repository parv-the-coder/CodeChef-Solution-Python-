# Initializing the rank variable
rank = int(input())

# Displaying the scholarship according to the given conditions
if rank >= 1 and rank <= 50:
    print("100")
elif rank > 50 and rank <= 100:
    print("50")
else:
    print("0")
